import {StyleSheet, Text, View, FlatList} from 'react-native';
import React from 'react';

const GalleryFlatList = () => {
  return (
    <View>
      <Text>GalleryFlatList</Text>
    </View>
  );
};

export default GalleryFlatList;

const styles = StyleSheet.create({});
